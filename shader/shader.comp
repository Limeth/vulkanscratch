#version 450

// local workgroup dimensions
layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(set = 0, binding = 0) uniform InputData {
    uvec2 input_vec;
} input_data;

layout(set = 0, binding = 1) buffer OutputData {
    uint array[];
} output_data;

void main() {
    output_data.array[gl_GlobalInvocationID.x] = gl_GlobalInvocationID.x % 2;
}
